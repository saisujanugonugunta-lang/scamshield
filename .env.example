# Backend secrets (set these in your hosting provider, not in the frontend)
GEMINI_API_KEY=
FIREBASE_SERVICE_ACCOUNT_JSON=
FRONTEND_URL=
# Optional only if frontend and backend are deployed separately:
# VITE_API_URL=https://your-backend.example.com
