{
  "name": "scamshield",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "install:all": "npm --prefix backend install --ignore-scripts && npm --prefix frontend install --ignore-scripts"
  }
}
